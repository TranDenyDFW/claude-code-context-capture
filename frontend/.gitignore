# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist is NOT ignored. See the note in the repository root .gitignore: the built page is
# committed so that running the dashboard needs no npm install, which is a headline promise
# of this project. npm is for CHANGING the frontend, not for running it.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
